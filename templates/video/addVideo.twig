{% extends 'base.html.twig' %}

{% block title %}Enregistrement{% endblock %}

{% block body %}
    {{ parent() }}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.published) }}
    {{ form_row(form.url) }}
    {{ form_row(form.title) }}
    {{ form_row(form.title) }}
    {{ form_row(form.title) }}


    {{ form_end(form) }}
{% endblock %}
